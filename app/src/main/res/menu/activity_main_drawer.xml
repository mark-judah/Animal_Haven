<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_shelters"
            android:icon="@drawable/ic_menu_camera"
            android:title="Animal Shelters"
            tools:ignore="HardcodedText" />
        <item
            android:id="@+id/nav_gallery"
            android:icon="@drawable/birds"
            android:title="Gallery"
            tools:ignore="HardcodedText" />
        <item
            android:id="@+id/nav_adoption"
            android:icon="@drawable/kittens"
            android:title="Get Your Pet Here"
            tools:ignore="HardcodedText" />
        <item
            android:id="@+id/nav_doc"
            android:icon="@drawable/ic_menu_manage"
            android:title="Vetinary Services"
            tools:ignore="HardcodedText" />
    </group>

    <item android:title="@string/communicate">
        <menu>
            <item
                android:id="@+id/nav_share"
                android:icon="@drawable/ic_menu_share"
                android:title="Share"
                tools:ignore="HardcodedText" />
            <item
                android:id="@+id/nav_send"
                android:icon="@drawable/ic_menu_send"
                android:title="Send"
                tools:ignore="HardcodedText" />
        </menu>
    </item>

</menu>
